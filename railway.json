{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pip install -r backend/requirements.txt && uvicorn backend.app.main:app --host 127.0.0.1 --port 8000 & sleep 3 && NODE_ENV=production API_TARGET=http://127.0.0.1:8000 node dist/server.cjs",
    "healthcheckPath": "/",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
